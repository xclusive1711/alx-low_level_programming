#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: checks for char
 * @b: checks for char
 * Return: returns multiplication of two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
